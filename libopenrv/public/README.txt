This directory contains all public headers that form the API of libopenrv.

These are the only files an application should need to include. Furthermore, these are the headers
that should be installed along with the library.

